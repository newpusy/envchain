{
  "name": "envchain",
  "version": "0.1.0",
  "description": "Lightweight utility to chain and validate environment variable configs across multiple .env files",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src/"
  },
  "keywords": ["env", "dotenv", "config", "environment", "validation"],
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
